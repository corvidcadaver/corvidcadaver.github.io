/** @type {import('next').NextConfig} */

const nextConfig = {
    // output: "standalone",
    // reactStrictMode: true,
    // experimental: {
    //     turbo: false
    // }
    output: 'export',
    assetPrefix: './'
};

export default nextConfig;
